  - name: Initialize nodes
    hosts: tpi1_nodes
    tasks:
      - name: Include "ubuntu" role
        include_role:
          name: ubuntu
        when: ansible_facts['distribution']|lower == 'ubuntu'
